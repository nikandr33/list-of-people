# Simple test app with crud functionality.

This application is test, was built with Angular 5 and Bootstrap 4. 

## Development server

Run `ng serve` for a dev server. Navigate to `http://localhost:4200/`.

